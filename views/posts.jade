extends layout
block content
        div.container(ng-controller='AppCtrl')
            h1 Posts
            ul(ng-repeat='post in posts')
                li
                    | {{post.title}}
                li
                    | {{post.author}}
                li
                    | {{post.body}}

block scripts
        script(src='js/app.js')

